<h1>{{title}}</h1>
<p>Welcome to {{title}}</p>

<div class="main">

    {{#repos}}
        {{#if visible}}
            <div class="box">
                {{name}}
                <div>
                    {{description}}
                </div>
                {{#if localNotes}}
                    <div>
                        {{localNotes}}
                    </div>
                {{/if}}
                <div class="links">
                    <div>
                        {{#if npm}}
                            See it on <a target="_blank" href="https://www.npmjs.com/package/{{name}}">npm</a>!
                        {{/if}}
                    </div>
                    <div>
                        <a target="_blank" href="{{html_url}}">GitHub</a>
                    </div>
                </div>

            </div>
        {{/if}}
    {{/repos}}

</div>
